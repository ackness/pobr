# pathofexile-dat 的原始产物与缓存：均为大体积中间物，绝不入库。
# 仅 config.json / 脚本 / README 入库；真源数据按需从 GGG CDN 拉取后适配为 data/ 下的最小 JSON。
.cache/
tables/
files/
# GGG 官方被动树导出原始 data.json（数 MB，绝不入库；适配后落 data/<patch>/passive_tree.json）
tree/
node_modules/
*.log
